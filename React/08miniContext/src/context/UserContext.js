import React from "react";

const UserContext=React.createContext();// Here Create Context is a method. UserContext is a variable that stores the context object.context is just like global variable.
export default UserContext

 